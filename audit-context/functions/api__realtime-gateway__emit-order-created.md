## `RealtimeGateway.emitOrderCreated` in apps/api/src/modules/realtime/realtime.gateway.ts (L19-L21)

**Purpose:** Broadcasts an `order.created` event with an order id and payload to every socket in the `global` room (L19-L21).

---

**Inputs & Assumptions:**
- `orderId` (`string`): generated by `OrderController.create` on the only visible caller path (`order.controller.ts:L40-L49`). Trust: semi-trusted internal value.
- `payload` (`Record<string, unknown>`): assembled internally by the visible caller, but the type permits keys including `orderId` (L8, L19; `order.controller.ts:L48-L51`). Trust: semi-trusted.
- Precondition: `server` has been assigned by Nest's `@WebSocketServer` lifecycle before the order handler calls this method; framework-established, with no local runtime check (L12-L13, L20).

---

**Outputs & Effects:** Emits `order.created` to room `global`; object spread occurs after the explicit `orderId`, so a payload `orderId` replaces the first value (L20). Return value from Socket.IO is discarded.

---

**Block-by-Block:**

```typescript
// L19-L21
emitOrderCreated(orderId: string, payload: Payload) {
  this.server.to("global").emit("order.created", { orderId, ...payload });
}
```
- **What:** Selects the room and broadcasts a merged event object. **Why here:** it is the method's sole effect. **Assumes:** payload keys are compatible with the event envelope and room membership is the intended audience; the current caller supplies the same order id twice (`order.controller.ts:L48-L50`). **Establishes:** an emit call is issued with event name and merged payload. **Depended on by:** connected global-room clients.

---

**Cross-Function Dependencies:**
- Callees `Server.to` and broadcast `emit` (external-black-box) at L20.
- Caller: `OrderController.create` only (`order.controller.ts:L48-L51`; repository search found no other call).
- Shared state: gateway `server` and `global` room populated by `handleConnection` (L12-L17).

---

**Open Questions:**
- unclear; need client event handlers/contracts; no repository listener for `order.created` was found.
- unclear; need delivery/retry expectations for Socket.IO emissions.

